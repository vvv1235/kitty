'use client'

// Esta página era uma versão mock de anúncio de pet.
// Agora ela simplesmente reutiliza a página real de anúncio do dashboard.

import AnnouncePet from '../(dashboard)/announce-pet/page';

export default function AddPetPage() {
  return <AnnouncePet />;
}
